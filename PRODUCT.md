# Product

## Register

product

## Users

在 Windows 上使用 Codex App、Codex CLI 或 VS Code Codex 扩展的开发者。他们需要在使用 Codex 工作时快速确认会话阶段、项目上下文和权限请求。

## Product Purpose

通过本机 Named Pipe 接收 Codex 生命周期 Hook，在一个常驻桌面窗口中展示会话状态，并将需要人工确认的权限请求转到独立审批窗口。成功意味着中文信息准确可读、不同生命周期阶段清晰可辨、审批反馈可靠。

## Brand Personality

清晰、克制、可靠。界面语气直接，优先帮助用户快速判断当前状态和下一步操作。

## Anti-references

避免装饰性视觉效果、含糊或重复的状态名称，以及仅依赖颜色传达关键信息。避免为了视觉变化重造标准桌面交互。

## Design Principles

1. 状态优先：生命周期阶段必须一眼可辨。
2. 原文可信：会话名称、工具参数和项目上下文必须保持原始编码与含义。
3. 熟悉可用：沿用 Windows 桌面应用的常见交互方式。
4. 最小干扰：监视器常驻但不打断工作，只有权限请求需要用户决策。

## Accessibility & Inclusion

中文文本必须完整显示并保持足够对比度；关键状态同时使用明确文字表达，不仅依赖颜色；交互保留键盘默认行为，并避免无意义动效。
